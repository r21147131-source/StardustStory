# Grok Image MCP Configuration
# Copy this file to .env and add your API key

# Free offline development — no API key or credits required
# GROK_IMAGE_MOCK=1

# Your xAI API key from https://console.x.ai
# Not needed when GROK_IMAGE_MOCK=1 or when using Grok subscription OAuth (grok login)
# XAI_API_KEY=your_xai_api_key_here

# Credential priority: auto (default), oauth, api_key
# GROK_IMAGE_AUTH=auto

# Custom path to Grok OAuth store (default: ~/.grok/auth.json)
# GROK_AUTH_JSON=/path/to/auth.json

# Optional: default image model
# GROK_IMAGE_MODEL=grok-imagine-image-quality

# Optional: custom directory for saved images
# GROK_IMAGES_DIR=/path/to/your/images

# Optional: enable request logging
# GROK_IMAGE_LOG_FILE=/tmp/grok-image-mcp.log